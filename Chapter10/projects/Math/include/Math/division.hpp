#pragma once

namespace Math
{
  /**
   * @brief   Divides two numbers.
   * @param   a The first number.
   * @param   b The second number.
   * @returns The quotient of the two numbers.
   */
  [[nodiscard]] auto divide(double a, double b) noexcept -> double;
}
