#pragma once

namespace Math
{
  /**
   * @brief   Subtracts two numbers.
   * @param   a The first number.
   * @param   b The second number.
   * @returns The difference of the two numbers.
   */
  [[nodiscard]] auto subtract(double a, double b) noexcept -> double;
}
