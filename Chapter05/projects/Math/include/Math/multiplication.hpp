#pragma once

namespace Math
{
  /**
   * @brief   Multiplies two numbers.
   * @param   a The first number.
   * @param   b The second number.
   * @returns The product of the two numbers.
   */
  [[nodiscard]] auto multiply(double a, double b) noexcept -> double;
}
